import 'package:flutter/material.dart';

class FlightReservationPage extends StatelessWidget {
  const FlightReservationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
